@AGENTS.md

## Claude Code 전용

- 코드 구조 탐색·콜체인·영향 범위는 codebase-memory 그래프 도구(`search_graph`, `trace_call_path`, `detect_changes`)를 먼저 쓰고, 그래프에 없는 문자열·설정값만 Grep/Read로 보완한다.
- 작업은 `origin/main`에서 `<type>/<slug>` 브랜치를 분기해 진행하고 커밋은 논리 단위로 나눈다. 커밋·푸시·PR 생성·범위 밖 수정·결과가 갈리는 설계 선택은 먼저 묻는다.
- 여러 파일에 걸치거나 접근이 불확실하면 계획을 먼저 보여주고 확인받는다. diff를 한 문장으로 말할 수 있으면 바로 구현한다.
- 순수 계산·스토어 상태 변환·파싱 로직은 테스트를 먼저 쓴다.
- 저장·복구(`state/`), 동시성, IPC 계약처럼 실패 비용이 큰 변경은 PR 전에 `/code-review`로 독립 리뷰를 받는다. 나머지는 CI의 Claude 리뷰에 맡긴다.
- `/agent-plan`·`/agent-review`·`/agent-debug`는 사용자가 독립 검증을 원할 때 직접 호출하는 스킬이다. 필요해 보이면 제안만 한다.
- 리뷰·조사 서브에이전트는 읽기 전용이다. `git checkout`·`stash`·`restore` 등 작업 트리를 바꾸는 명령을 쓰지 않는다. 미커밋 변경은 리뷰 대상이다.
- 리뷰는 정확성이나 명시된 요구사항에 영향을 주는 문제만 보고한다. 방어 코드·에러 핸들링 강화 제안은 버그가 아니면 생략한다. `let _ = store.update(...)`처럼 결과를 의도적으로 무시하는 패턴은 컨벤션이다.
